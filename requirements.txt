# Python dependencies for the data-processing scripts under web/.
# Developed with Python 3.11. Install with:  pip install -r requirements.txt
#
# The interactive map itself (web/map.html) is STATIC and needs no Python — serve the
# web/ directory with any static file server (see "Building and reproducing" in the README).

geopandas==1.1.3
pandas==2.2.2
numpy==1.26.4
shapely==2.1.2
pyproj==3.7.2
pyshp==3.1.4
pdfplumber==0.11.10
